(* send.pv

Message 1: A -> B : RSA

*)

channel c. (* free c : channel. *)
free Cocks : bitstring[private].
free RSA : bitstring[private].

query attacker(RSA).
query attacker(Cocks).

process
    out(c, RSA)
